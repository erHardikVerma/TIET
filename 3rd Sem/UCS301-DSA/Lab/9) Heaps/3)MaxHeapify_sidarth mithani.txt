max_heapify(arr,i)
{
    largest=i;
    left=2i;
    right=2i+1;
    if(left<n&&arr[left]>arr[i])
        largest=left
    if(right<n&&arr[right]>arr[i])
        largest=right
    if(largest!=i)
    {
        swap(arr[i],arr[largest])
        max_heapify(arr,largest)
    }
}
